#Primer acercamiento al codigo del proyecto, primera prueba.
base_de_datos = []
print("Escribe 1 para ingresar informacion de nuevas jugadas.")
print("Escribe 2 para ver jugadas previamente hechas, y revisar porcentajes, aciertos etc.")
Res1 = int(input(""))
if Res1 == 1:
    print("Formaciones a elegir: 1. Escopeta  2. Pistola   3. I-Form   4- Singleback")
    Formacion= int(input("Ingresa el numero de la formación: "))
    print("Ingresa el numero de down:")
    Down= int(input(""))
    print("Ingresa el yardaje por conseguir")
    Yardas= int(input(""))
    print("¿La jugada fue un:  1.pase  o  2.acarreo?")
    Jugada= int(input(""))
    jugada_registrada = {
        "formacion": Formacion,
        "down": Down,
        "yardas": Yardas,
        "tipo": Jugada
    }
    base_de_datos.append(jugada_registrada)

elif Res1 == 2:
    print("CONSULTA DE TENDENCIAS OFENSIVAS")
    
    if len(base_de_datos) == 0:
        print("No hay jugadas registradas en el sistema aún.")
    else:
        print("Formaciones a consultar: 1. Escopeta  2. Pistola  3. I-Form  4. Singleback")
        formacion_buscar = int(input("Ingresa la formación que deseas analizar: "))
        down_buscar = int(input("Ingresa el down que deseas analizar (1-4): "))

        coincidencias = 0
        total_pases = 0
        total_carreras = 0
        
        for jugada in base_de_datos:

            if jugada["formacion"] == formacion_buscar and jugada["down"] == down_buscar:
                coincidencias = coincidencias + 1
                
                if jugada["tipo"] == 1:
                    total_pases = total_pases + 1
                elif jugada["tipo"] == 2:
                    total_carreras = total_carreras + 1
